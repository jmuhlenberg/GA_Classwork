<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="style.css">
    </head>
    <body>
      <div class="container">
        <h1>Mongoose Store</h1>
        <nav>
          <a href="/store/new">Create a New Item</a>
        </nav>
        <div class="index-container">
            <% for(i=0; i<items.length; i++){ %>
                <div class='index-item'>
                  <a class='index-name' href='/store/<%=items[i].id%>'><%=items[i].name%></a><br/>
                  <span>Price: $<%=items[i].price%></span>
                  <span>Quantity: <%=items[i].quantity%></span>
                  <br/>
                  <img class='index-image' src="<%=items[i].image%>" alt="<%=items[i].name%>">
                  <p>Description: <%=items[i].details%></p>
                  <a class='index-edit' href="/store/<%=items[i].id%>/edit">EDIT</a>
                  <form action="/store/<%=items[i].id%>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                  </form>
                  <% if(items[i].quantity > 0){ %>
                    <form class='buy' action="/store/<%=items[i].id%>/buy?_method=PUT" method="POST">
                      <input type="submit" class='buy' value="BUY">
                    </form>
                  <% } else {%>
                    <span>out of stock</span>
                  <% } %>
                </div>
            <% } %>
        </div>
      </div>
    </body>
</html>
